name: Daily Rule Cleanup

on:
  schedule:
    # 每日北京时间 09:00 (UTC 01:00) 运行
    - cron: '0 1 * * *'
  workflow_dispatch: # 允许手动触发调试

jobs:
  update_rules:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Modify Rule Sets
        run: |
          # 修改 Ads_AWAvenue.list 和 Ads_SukkaW.list
          # 删除包含 DOMAIN-SUFFIX,stat.y.qq.com 的行
          sed -i '/DOMAIN-SUFFIX,stat.y.qq.com/d' Rules/Ads_AWAvenue.list
          sed -i '/DOMAIN-SUFFIX,stat.y.qq.com/d' Rules/Ads_SukkaW.list

          # 修改 domainset_reject.conf
          # 删除包含 .stat.y.qq.com 的行
          sed -i '/\.stat\.y\.qq\.com/d' Rules/domainset_reject.conf

      - name: Commit and Push
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 检查是否有文件变动
          if [ -n "$(git status --porcelain)" ]; then
            git add .
            git commit -m "Auto Update: Remove tracking domains at $(date +'%Y-%m-%d %H:%M')"
            git push
          else
            echo "No changes detected, skipping commit."
          fi
